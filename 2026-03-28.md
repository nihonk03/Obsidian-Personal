Escenario actual:
- Tengo un host en hostinger 
- Tengo mi dominio principal llamado batidospitaya.com y otros subdominios como api.batidospitaya.com , erp.batiospitaya.com, talento.batidospitaya.com, etc 
- Todos los subdominio tienen sus propias carpetas y archivos que se almacenan en github y a traves de github actions cada vez que se recibe un push lo sube al host
- Los subdominios tienen una serie de carpetas que se sube de github al host con ciertas exclusiones
- La carpeta core/ esta en el host de cada subdominio y tiene la siguiente infrarestructura :
  Domains/erp.batidospitaya.com/public_html/core/
  Domains/api.batidospitaya.com/public_html/core/
  Domains/talento.batidospitaya.com/public_html/core/
- En mi carpeta local tiene la siguiente infraestructura
  VIsualCode/erp.batidospitaya.com/core/
- VIsualCode/erp.batidospitaya.com/docs/
- VIsualCode/erp.batidospitaya.com/.agent/
  VIsualCode/api.batidospitaya.com/core/
- - VIsualCode/api.batidospitaya.com/docs/
- VIsualCode/api.batidospitaya.com/.agent/
  VIsualCode/talento.batidospitaya.com/core/
- - VIsualCode/talento.batidospitaya.com/docs/
- VIsualCode/talento.batidospitaya.com/.agent/
  VIsualCode/PitayaCore/core/
- VIsualCode/PitayaCore/docs/
- VIsualCode/PitayaCore/.agent/
- Los repositorios son los sighuientes:
  MiguelGotea/erp.batidospitaya.com/
  MiguelGotea/api.batidospitaya.com/
  MiguelGotea/talento.batidospitaya.com/
- Actualmente el core no se sube a github , esta excluido y solo queda en local dentro de cada subdominio porque todos los subdominios lo usan
- He crado un repositorio nuevo llamadao PitayaCore para almacenar el core
- Cada subdominio en local tiene .github/workflows/ con el respectivo Github Actions, adjunto los 3 :
  
```
name: 🚀 Deploy ERP - 17 Módulos

  

on:

  push:

    branches: [ main ]

  

jobs:

  deploy:

    runs-on: ubuntu-latest

    steps:

    - name: 📥 Checkout código

      uses: actions/checkout@v4

    - name: 🔑 Configurar SSH

      uses: webfactory/ssh-agent@v0.8.0

      with:

        ssh-private-key: ${{ secrets.HOSTINGER_SSH_KEY }}

    - name: 📤 Sincronizar Módulos ERP

      run: |

        # Definir lista de módulos

        MODULES=(

          "atencioncliente"

          "auxiliaradministrativo"

          "cds"

          "desarrollo"

          "diseno"

          "experienciadigital"

          "gerencia"

          "compras"

          "infraestructura"

          "legal"

          "mantenimiento"

          "marketing"

          "produccion"

          "rh"

          "sistemas"

          "sucursales"

          "tecnicodesarrollohumano"

          "ventas"

        )

        for MODULE in "${MODULES[@]}"; do

          if [ -d "./modulos/$MODULE" ]; then

            echo "🔄 Sincronizando modulos/$MODULE/"

            # Crear la carpeta si no existe (incluyendo uploads)

            ssh -o StrictHostKeyChecking=no -p 65002 ${{ secrets.HOSTINGER_USER }}@${{ secrets.HOSTINGER_HOST }} \

              "mkdir -p ${{ secrets.HOSTINGER_PATH }}/modulos/$MODULE && \

               mkdir -p ${{ secrets.HOSTINGER_PATH }}/modulos/$MODULE/uploads"

            # Sincronizar la carpeta excluyendo uploads

            rsync -avz \

              --delete \

              --exclude='uploads/' \

              --exclude='.gitignore' \

              --exclude='.agent/' \

              --exclude='core/' \

              --exclude='.scripts/' \

              --exclude='docs/' \

              -e "ssh -o StrictHostKeyChecking=no -p 65002" \

              ./modulos/$MODULE/ \

              ${{ secrets.HOSTINGER_USER }}@${{ secrets.HOSTINGER_HOST }}:${{ secrets.HOSTINGER_PATH }}/modulos/$MODULE/

          else

            echo "⚠️ Saltando modulos/$MODULE/ (no existe localmente)"

          fi

        done

    - name: 📤 Sincronizar README raíz

      run: |

        echo "🔄 Sincronizando README.md..."

        rsync -avz \

          -e "ssh -o StrictHostKeyChecking=no -p 65002" \

          ./README.md \

          ${{ secrets.HOSTINGER_USER }}@${{ secrets.HOSTINGER_HOST }}:${{ secrets.HOSTINGER_PATH }}/

    - name: 🔧 Configurar permisos

      run: |

        # Definir lista de módulos de nuevo para el paso de permisos

        MODULES=(

          "atencioncliente"

          "auxiliaradministrativo"

          "cds"

          "desarrollo"

          "diseno"

          "experienciadigital"

          "gerencia"

          "compras"

          "infraestructura"

          "legal"

          "mantenimiento"

          "marketing"

          "produccion"

          "rh"

          "sistemas"

          "sucursales"

          "tecnicodesarrollohumano"

          "ventas"

        )

        for MODULE in "${MODULES[@]}"; do

          if [ -d "./modulos/$MODULE" ]; then

            echo "🔧 Configurando permisos para modulos/$MODULE/"

            ssh -o StrictHostKeyChecking=no -p 65002 ${{ secrets.HOSTINGER_USER }}@${{ secrets.HOSTINGER_HOST }} \

              "chmod -R 755 ${{ secrets.HOSTINGER_PATH }}/modulos/$MODULE/ && \

               find ${{ secrets.HOSTINGER_PATH }}/modulos/$MODULE/ -type f -exec chmod 644 {} \; && \

               chmod -R 755 ${{ secrets.HOSTINGER_PATH }}/modulos/$MODULE/uploads/ && \

               chmod -R 777 ${{ secrets.HOSTINGER_PATH }}/modulos/$MODULE/uploads/ || true"

          else

            echo "⚠️ Saltando permisos para modulos/$MODULE/ (no existe localmente)"

          fi

        done

    - name: ✅ Verificar deploy

      run: |

        echo "✅ Deploy completado para los 17 módulos"

        echo "📁 Carpetas 'uploads' excluidas del sync"

        echo "🌐 Sitio: https://erp.batidospitaya.com"

        echo "🖥️ Servidor: ${{ secrets.HOSTINGER_HOST }}:65002"
```

```
name: 🚀 Deploy API - Batidos Pitaya

  

on:

  push:

    branches: [ main ]

  

jobs:

  deploy:

    runs-on: ubuntu-latest

    steps:

    - name: 📥 Checkout código

      uses: actions/checkout@v4

    - name: 🔑 Configurar SSH

      uses: webfactory/ssh-agent@v0.8.0

      with:

        ssh-private-key: ${{ secrets.HOSTINGER_SSH_KEY }}

    - name: 📤 Sincronizar archivos (rsync)

      run: |

        echo "🔄 Sincronizando repositorio..."

        rsync -avz \

          --delete \

          --exclude='.git/' \

          --exclude='.github/' \

          --exclude='.scripts/' \

          --exclude='.agent/' \

          --exclude='.gitignore' \

          --exclude='core/' \

          --exclude='docs/' \

          --exclude='api/uploads/' \

          -e "ssh -o StrictHostKeyChecking=no -p 65002" \

          ./ \

          ${{ secrets.HOSTINGER_USER }}@${{ secrets.HOSTINGER_HOST }}:${{ secrets.HOSTINGER_PATH_API }}/

    - name: 🔧 Configurar permisos

      run: |

        echo "🔧 Configurando permisos en el servidor..."

        ssh -o StrictHostKeyChecking=no -p 65002 ${{ secrets.HOSTINGER_USER }}@${{ secrets.HOSTINGER_HOST }} << 'EOF'

          cd ${{ secrets.HOSTINGER_PATH_API }}

          find api -type d -exec chmod 755 {} \;

          find api -type f -exec chmod 644 {} \;

          chmod -R 777 api/uploads/ || true

        EOF

    - name: ✅ Verificar deploy

      run: |

        echo "✅ Deploy completado exitosamente"

        echo "📁 Carpeta sincronizada: api/"

        echo "📁 Carpeta excluida: api/uploads/"

        echo "🌐 Sitio: https://api.batidospitaya.com"

        echo "🖥️ Servidor: ${{ secrets.HOSTINGER_HOST }}:65002"
```

```
name: 🚀 Deploy Talento - Batidos Pitaya

  

on:

  push:

    branches: [ main ]

  

jobs:

  deploy:

    runs-on: ubuntu-latest

    steps:

    - name: 📥 Checkout código

      uses: actions/checkout@v4

    - name: 🔑 Configurar SSH

      uses: webfactory/ssh-agent@v0.8.0

      with:

        ssh-private-key: ${{ secrets.HOSTINGER_SSH_KEY }}

    - name: 📤 Sincronizar archivos (rsync)

      run: |

        echo "🔄 Sincronizando archivos..."

        rsync -avz \

          --delete \

          --exclude='.git/' \

          --exclude='.github/' \

          --exclude='.gitignore' \

          --exclude='.agent/' \

          --exclude='.scripts/' \

          --exclude='core/' \

          --exclude='docs/' \

          --exclude='uploads/' \

          -e "ssh -o StrictHostKeyChecking=no -p 65002" \

          ./ \

          ${{ secrets.HOSTINGER_USER }}@${{ secrets.HOSTINGER_HOST }}:${{ secrets.HOSTINGER_PATH_TALENTO }}/

    - name: 🔧 Configurar permisos

      run: |

        echo "🔧 Configurando permisos en el servidor..."

        ssh -o StrictHostKeyChecking=no -p 65002 ${{ secrets.HOSTINGER_USER }}@${{ secrets.HOSTINGER_HOST }} << 'EOF'

          cd ${{ secrets.HOSTINGER_PATH_TALENTO }}

          # Directorios a 755

          find . -maxdepth 1 -type d ! -path "." -exec chmod 755 {} \;

          # Archivos a 644

          find . -maxdepth 1 -type f -exec chmod 644 {} \;

          # Carpetas específicas (si existen y se sincronizaron)

          [ -d "ajax" ] && chmod 755 ajax && find ajax -type f -exec chmod 644 {} \;

          [ -d "assets" ] && chmod 755 assets && find assets -type f -exec chmod 644 {} \;

          [ -d "css" ] && chmod 755 css && find css -type f -exec chmod 644 {} \;

          [ -d "js" ] && chmod 755 js && find js -type f -exec chmod 644 {} \;

          echo "✅ Permisos actualizados."

        EOF

    - name: ✅ Verificar deploy

      run: |

        echo "✅ Deploy completado exitosamente para talento.batidospitaya.com"
```


El problema que tengo es que en los subdominios esta una carpeta llamada core con todos los archivos de auth, vendors, phpmail, herramientas globales, etc y esto lo tengo que copiar en todos los subdominios cada vez que hago un cambio de esa carpeta porque todas la usan pero esta alojada en todos los subdominios

Lo que necesito es tener un repositorio PitayaCore donde tenga las 3 carpetas que tengo actualmente core/, .agent y docs/ y funcione de la siguiente manera:

1. Cuando modifico directamente dentro de VisualCode/PitayaCore/ y hago push se suba al repositorio las 3 carpetas (docs, .agent y core), una vez subido se replique las 3 carpetas a los 3 repositorios; luego cada repositorio suba unicamente la carpeta core/ al host de cada subdominio 
2. Cuando entro al proyecto local de un subdominio (por ejemplo VisualCode/erp.batidospitaya.com/) y trate de hacer algun cambio detecte si hubo algun cambio en la repo de core/ para hacer primero un pull al local asi tener en local la ultima version de core/ dentro del proyecto
3. Cuando estoy trabajando en un proyecto local y tengo que hacer algun cambio en el core/, .agent/ y docs  de manera local, cuando hago push detecte que el repositorio PitayaCore estaria descautlizado entonces aparte de hacer push de esas 3 carpetas al github de es eproyecto haga el push al repoditorio PitayaCore y se active lo mismo del paso 1 que replique a los otros github y suba al host solo core/
   
Que soluciones profesionales y alineados a futuras expansiones de mas subdominios tengo? algo totalmente profesional y escalable, hazme todas las preguntas necesarias