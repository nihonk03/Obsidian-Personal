SOlicitud vacaiones  / Libres
Solicitud de Feriados 
Celulares nuevo servicio para cambiar de miradone 
Spotify distribuir cuenta de spotify 


Informe semanal
- boton de reembolos de facturas
- historial de marcaciones

rrhh
- qr de formulario digital directo sin pasar por registro desde cv
- boton omitir entrevista de jefe inmediato para pasar directo a postalnte seldciconado


Triple Berry EnTienda Mediano 110Mv24



Necesito har un informe de balance de cierre de caja diario omo esta estructurado en access, esos datos ya se suben a la web asi que podemos armar este mismo informe en una pagina La herramienta estara en el modulo de contabilidad erp.batidospitaya.com\modulos\contabilidad Se creara un tool llamado balance_cierre_diario donde tendra el permiso vista unicamente para acceder a la herrameinta Las tablas implicadas son las siguientes: CREATE TABLE `VentasGlobalesAccessCSV` ( `Anulado` int(11) DEFAULT NULL, `MotivoAnulado` text DEFAULT NULL, `Fecha` date DEFAULT NULL, `Hora` time DEFAULT NULL, `CodPedido` int(11) DEFAULT NULL, `CodCliente` int(11) DEFAULT NULL, `aPOS` varchar(30) DEFAULT NULL, `Delivery_Nombre` varchar(30) DEFAULT NULL, `Tipo` varchar(30) DEFAULT NULL, `NombreGrupo` varchar(30) DEFAULT NULL, `DBBatidos_Nombre` varchar(100) DEFAULT NULL, `Medida` varchar(30) DEFAULT NULL, `Cantidad` double DEFAULT NULL, `CodigoPromocion` int(11) DEFAULT NULL, `Precio` double DEFAULT NULL, `local` varchar(11) DEFAULT NULL, `Caja` varchar(100) DEFAULT NULL, `Modalidad` varchar(30) DEFAULT NULL, `Motorizado` varchar(100) DEFAULT NULL, `Observaciones` text DEFAULT NULL, `Precio_Unitario_Sin_Descuento` double DEFAULT NULL, `Impresiones` int(11) DEFAULT NULL, `HoraCreado` time DEFAULT NULL, `HoraIngresoProducto` time DEFAULT NULL, `HoraImpreso` time DEFAULT NULL, `Propina` double DEFAULT NULL, `Semana` int(11) DEFAULT NULL, `Puntos` double DEFAULT NULL, `CodProducto` varchar(20) DEFAULT NULL, `MontoFactura` int(11) DEFAULT NULL, `Sucursal_Nombre` varchar(20) DEFAULT NULL, `PedidoDeCentral` int(11) DEFAULT NULL, `CodMotorizado` int(11) DEFAULT NULL, KEY `idx_fecha` (`Fecha`), KEY `idx_codproducto` (`CodProducto`), KEY `idx_fecha_hora` (`Fecha`,`Hora`), KEY `idx_sucursal` (`Sucursal_Nombre`), KEY `idx_membresia` (`CodCliente`), KEY `idx_rfm_fecha` (`Fecha`), KEY `idx_rfm_cliente_pedido` (`CodCliente`,`CodPedido`), KEY `idx_rfm_anulado_tipo` (`Anulado`,`Tipo`), KEY `idx_rfm_promocion` (`CodigoPromocion`), KEY `idx_rfm_cod_cliente` (`CodCliente`), KEY `idx_rfm_cod_pedido` (`CodPedido`), KEY `idx_rfm_status` (`Anulado`), KEY `idx_rfm_sucursal` (`Sucursal_Nombre`), KEY `idx_rfm_socio_actividad` (`CodCliente`,`Fecha`), KEY `idx_codpedido` (`CodPedido`), KEY `idx_codcliente` (`CodCliente`), KEY `idx_apos` (`aPOS`), KEY `idx_delivery_nombre` (`Delivery_Nombre`), KEY `idx_tipo` (`Tipo`), KEY `idx_nombregrupo` (`NombreGrupo`), KEY `idx_db_batidos_nombre` (`DBBatidos_Nombre`), KEY `idx_medida` (`Medida`), KEY `idx_local` (`local`), KEY `idx_caja` (`Caja`), KEY `idx_modalidad` (`Modalidad`), KEY `idx_motorizado` (`Motorizado`), KEY `idx_horacreado` (`HoraCreado`), KEY `idx_horaingresoproducto` (`HoraIngresoProducto`), KEY `idx_horaimpreso` (`HoraImpreso`), KEY `idx_semana` (`Semana`), KEY `idx_puntos` (`Puntos`), KEY `idx_montofactura` (`MontoFactura`), KEY `idx_pedidodecentral` (`PedidoDeCentral`), KEY `idx_codmotorizado` (`CodMotorizado`), KEY `idx_consumo_semana_prod` (`Semana`,`CodProducto`,`Anulado`), KEY `idx_consumo_local_semana` (`local`,`Semana`,`Anulado`) ) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci ROW_FORMAT=DYNAMIC; CREATE TABLE `msaccess_masivo_CierreDiario` ( `id_host` int(11) NOT NULL AUTO_INCREMENT, `Sucursal` int(11) NOT NULL, `CodigoCierre` int(11) NOT NULL, `HoraInicial` time DEFAULT NULL, `HoraFinal` time DEFAULT NULL, `Fecha` date DEFAULT NULL, `CodOperario` int(11) DEFAULT NULL, `MFCor` double DEFAULT NULL, `MFDol` double DEFAULT NULL, `Faltante` int(11) DEFAULT NULL, `TotalHugo` double DEFAULT NULL, `TotalPedidosYa` double DEFAULT NULL, `TotalTransferencia` double DEFAULT NULL, `TotalPOS` double DEFAULT NULL, `Observaciones` varchar(255) DEFAULT NULL, `FechaUltimoSync` datetime DEFAULT NULL, PRIMARY KEY (`id_host`), UNIQUE KEY `uk_suc_cod` (`Sucursal`,`CodigoCierre`), KEY `idx_fecha` (`Fecha`), KEY `idx_sucursal` (`Sucursal`) ) ENGINE=InnoDB AUTO_INCREMENT=4715 DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci; CREATE TABLE `msaccess_masivo_Depositos` ( `id_host` int(11) NOT NULL AUTO_INCREMENT, `Sucursal` int(11) NOT NULL, `CodDeposito` int(11) NOT NULL, `Monto` int(11) DEFAULT NULL, `Denominacion` varchar(255) DEFAULT NULL, `Tipo` varchar(255) DEFAULT NULL, `Fecha` date DEFAULT NULL, `Observacion` varchar(255) DEFAULT NULL, `DuranteTurno` tinyint(4) DEFAULT NULL, `FechaUltimoSync` datetime DEFAULT NULL, PRIMARY KEY (`id_host`), UNIQUE KEY `uk_suc_cod` (`Sucursal`,`CodDeposito`), KEY `idx_fecha` (`Fecha`), KEY `idx_sucursal` (`Sucursal`) ) ENGINE=InnoDB AUTO_INCREMENT=1023 DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci; CREATE TABLE `msaccess_masivo_Compras` ( `id_host` int(11) NOT NULL AUTO_INCREMENT, `Sucursal` int(11) NOT NULL, `CodIngresoAlmacen` int(11) NOT NULL, `CodCotizacion` int(11) DEFAULT NULL, `Cantidad` double DEFAULT NULL, `Fecha` date DEFAULT NULL, `CostoTotal` double DEFAULT NULL, `Observaciones` varchar(255) DEFAULT NULL, `CodProveedor` int(11) DEFAULT NULL, `Destino` varchar(255) DEFAULT NULL, `Tipo` varchar(255) DEFAULT NULL, `Pagado` date DEFAULT NULL, `NumeroFactura` varchar(255) DEFAULT NULL, `CodOperario` int(11) DEFAULT NULL, `Ingresado` tinyint(4) DEFAULT NULL, `Lote` double DEFAULT NULL, `Peso` double DEFAULT NULL, `FechaUltimoSync` datetime DEFAULT NULL, PRIMARY KEY (`id_host`), UNIQUE KEY `uk_suc_cod` (`Sucursal`,`CodIngresoAlmacen`), KEY `idx_fecha` (`Fecha`), KEY `idx_sucursal` (`Sucursal`) ) ENGINE=InnoDB AUTO_INCREMENT=159748 DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci; CREATE TABLE `Operarios` ( `CodOperario` int(11) NOT NULL AUTO_INCREMENT, `Nombre` varchar(255) DEFAULT NULL, `Nombre2` varchar(255) DEFAULT NULL, `Apellido` varchar(255) DEFAULT NULL, `Apellido2` varchar(255) DEFAULT NULL, `clave` varchar(255) DEFAULT NULL COMMENT 'Contraseña sistema', `clave_hash` varchar(255) DEFAULT NULL COMMENT 'Contraseña bcrypt', `Operativo` bit(1) DEFAULT NULL, `Celular` varchar(255) DEFAULT NULL, `Cedula` varchar(255) DEFAULT NULL, `Genero` enum('M','F','O') DEFAULT NULL, `Inicio` datetime DEFAULT NULL, `Fin` datetime DEFAULT NULL, `Cumpleanos` datetime DEFAULT NULL, `Sucursal` int(11) DEFAULT NULL, `Ciudad` varchar(255) DEFAULT NULL, `CodClub` int(11) DEFAULT NULL, `Cargo` varchar(255) DEFAULT NULL, `FechaRegistro` datetime DEFAULT NULL, `usuario` varchar(100) DEFAULT NULL COMMENT 'Usuario en sistema', `cb_numero` varchar(100) DEFAULT NULL, `contacto_numero` int(11) DEFAULT NULL, `contacto_nombre` varchar(250) DEFAULT NULL, `direccion` varchar(250) DEFAULT NULL, `telefono_casa` varchar(20) DEFAULT NULL, `telefono_corporativo` varchar(20) DEFAULT NULL, `email_personal` varchar(255) DEFAULT NULL, `email_trabajo` varchar(255) DEFAULT NULL, `email_trabajo_clave` varchar(100) DEFAULT NULL, `foto_perfil` varchar(255) DEFAULT NULL, `codigo_inss` varchar(50) DEFAULT NULL, `segurosocial` varchar(20) DEFAULT NULL, `InicioSeguro` date DEFAULT NULL COMMENT 'Fecha de inicio del seguro social', `FinSeguro` date DEFAULT NULL COMMENT 'Fecha de fin del seguro social', `hospital_riesgo_laboral` varchar(255) DEFAULT NULL, `cb_titular` varchar(100) DEFAULT NULL, `cb_banco` varchar(100) DEFAULT NULL, `cb_moneda` varchar(20) DEFAULT NULL, `registrado_por` int(11) DEFAULT NULL, `fecha_hora_regsys` timestamp NOT NULL DEFAULT current_timestamp() COMMENT 'Fecha y hora de registro', `FechaCreacion` timestamp NULL DEFAULT NULL, `bot_github_token` varchar(500) DEFAULT NULL COMMENT 'Token GitHub cifrado AES para vault Obsidian', `bot_github_repo` varchar(200) DEFAULT NULL COMMENT 'Repositorio Obsidian: owner/repo', `bot_github_branch` varchar(50) DEFAULT 'main' COMMENT 'Rama del vault de Obsidian', `bot_github_vault_folder` varchar(100) DEFAULT '' COMMENT 'Carpeta raíz dentro del vault (vacío = raíz)', `bot_lid` varchar(50) DEFAULT NULL, `cantidad_hijos` int(11) DEFAULT NULL COMMENT 'Cantidad de hijos del colaborador', `talla_camisa` varchar(10) DEFAULT NULL COMMENT 'Talla de camisa (XS, S, M, L, XL, XXL)', PRIMARY KEY (`CodOperario`), KEY `fk_operarios_registrado_por` (`registrado_por`), KEY `idx_operarios_bot_lid` (`bot_lid`), KEY `idx_operarios_usuario` (`usuario`), CONSTRAINT `fk_operarios_registrado_por` FOREIGN KEY (`registrado_por`) REFERENCES `Operarios` (`CodOperario`) ON UPDATE CASCADE ) ENGINE=InnoDB AUTO_INCREMENT=834 DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;  
La tabla principal es la de cierre y la revision en el harreramienta sera por dia y sucursal, osea que selecciono la fecha y selecciono la sucursal con los datos msaccess_masivo_CierreDiario.Fecha y msaccess_masivo_CierreDiario.Sucursal , me filtrara todos los cierres que se hicieron en ese dia y lo miraremos como una lista que al seleccionar me cargara los datos de dicho cierre donde los valores son los siguientes: - COdigo de cierre: msaccess_masivo_CierreDiario.CodigoCierre - Cajero : BUscar en la tabla Operarios.CodOperario el codigo msaccess_masivo_CierreDiario.CodOperario para msotrar el nombre completo  
- Fecha: msaccess_masivo_CierreDiario.Fecha  
- Boleta Fisico: pos, trasnferencia , pedidosya son msaccess_masivo_CierreDiario. TotalPOS, TotalTransferencia, TotalPedidosYa  
- Boleta FIsica Efectivo : Conteo de Caja Total - Caja Inicial + Aligeramientos + Compras  
- Boleta Fisico Total Ventas : suma de la columna  
- Sistema Pitaya : pos, trasnferencia, pedidosya, efectivo se hace el calculo de la tabla VentasGlobalesAccessCSV.Modalidad donde los valores son POS, TRANSFERENCIA, PEDIDOSYA y EFECTIVO respectivamente y se filtra por .Fecha la del cierre y .Hora es menor o igual a msaccess_masivo_CierreDiario.HoraFinal  
- Sistema Pitaya TOtal Ventas = SUma de la columna  
- Diferencia: COlumna SIstema Pitaya - Boleta FIsico , SI es negativo es faltante, si es positivo es sobrante  
- Caja Inicial : msaccess_masivo_CierreDiario.MFCor+
